# Minimal send pack

If you only use three things from this research, use these.

## 1) First DM

Henry — I dug into Soar and my read is that the hard part is not “flight search,” it is making booking feel Uber-simple while managing Duffel offers/holds, payment, verification, booking progress/results, support, fraud, and attribution.

I’ve built adjacent workflow-heavy infra solo in Tailorec: Next.js + FastAPI + agent/runtime + analytics + AWS ops, plus a 32-bit OS from scratch.

If useful, I can send a 1-page public-signal teardown on shared fare → hold → ticketed booking and the internal tools I’d build first.

## 2) If he says “send it”

Send `research/henry_followup_teardown.md` with this note:

“Here’s the teardown. It is intentionally based only on public product/legal/client-route signals, so I may be wrong on implementation details. The workflow risks I’d validate first are booking state, payment/verification ambiguity, support tooling, and deal-link conversion.”

## 3) If he asks “what would you ship?”

I’d ship a 48-hour, no-private-access artifact:

- event schema for shared fare → hold → ticketed booking;
- fake-data booking timeline/admin console;
- metric tree for activation, intent, conversion, trust, and growth;
- implementation questions before touching production.

## 4) If he asks “why you?”

Because my strongest work is exactly the messy middle: product UX, backend workflows, event traces, runtime/tooling systems, analytics, and production ops. Tailorec proves the product/workflow layer; the OS project proves I can debug below frameworks when needed.

## 5) Stop rule

If he does not respond after one short follow-up, stop bumping. Build the 48-hour artifact independently only if it will be genuinely good enough to send as proof of work.
