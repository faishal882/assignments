{
  "name": "buildable-land-analysis",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "postinstall": "node scripts/setup-backend.mjs",
    "dev": "node scripts/dev.mjs",
    "test": "npm run test --workspace frontend && backend/.venv/bin/python -m pytest backend/tests",
    "build": "npm run build --workspace frontend"
  }
}
